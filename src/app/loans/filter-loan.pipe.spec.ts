import { FilterLoanPipe } from './filter-loan.pipe';

describe('FilterLoanPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterLoanPipe();
    expect(pipe).toBeTruthy();
  });
});
